[Unit]
Description=VAPN routing snapshot build schedule

[Timer]
# RIPE RIS publishes bviews every 8h (00/08/16 UTC); build 30min after.
OnCalendar=00,08,16:30 UTC
RandomizedDelaySec=10m
Persistent=true

[Install]
WantedBy=timers.target
